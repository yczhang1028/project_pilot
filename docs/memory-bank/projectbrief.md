# Project Brief

Project Pilot is a VS Code extension for managing local folders, workspace files, SSH remote folders, and SSH workspace files from a unified UI.

Core goals:
- Make project switching faster inside VS Code
- Keep project metadata stored locally and portable
- Support both local and remote workflows without requiring the extension on remote hosts
- Provide a modern React-based management UI plus an outline/tree view
