# Tech Context

- Main extension: TypeScript, CommonJS, compiled from `src` to `out`
- Webview UI: React 18 + TypeScript + Vite in `webview-ui`
- Build commands:
  - `npm run build`
  - `npm run build:ext`
  - `npm run build:webview`
- No dedicated lint script is configured; diagnostics should be checked with editor lints after edits
